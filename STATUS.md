# STATUS — 开发进度实时追踪

> **本文件用途**：AI Agent 集群交接的唯一进度入口。任何 agent 接手前先读本文件，即可对齐项目定位、当前进度与下一步工作。
> **更新约定**：每完成一个开发里程碑（功能/重构/修复批次）后，在「最新进展」顶部追加一条，并同步刷新「当前状态快照」与「待办」。

**最后更新**: 2026-08-30 · **A 批 2 + B 批 1 + 验收热修（转关小地图幽灵叠加修复 + 庭院东墙大门贴花）用户实机验收通过，已合入 master（`2616d4b`，回归 131/131 全绿）；下一阶段双线开工：A 批 3 二层夹层试点 / B 批 2 街道建筑实装 + 马路地面断面（守门方设计中）** · 远程 `origin/master` @ https://github.com/Nagi-226/before-the-silence

---

## 项目定位（30 秒版）

静默之前（Before the Silence，原名 Signal Lost）是复古 DOOM 风 FPS：2026 年 EDAA 二级探员「提灯」渗透城区仓库区（外星寄生孢子 AE-219 最早坠落点），穿越寄生体围攻抵达撤离信标——《Snuffers》（熄烛者 01 队雷暴重装清剿）的**前传**。仓库为**双引擎**结构：

- **C++ 原版**（SDL2 + C++17，raycasting 伪 3D）：功能完备、127 测试通过，现为**受保护归档**，不再加新特性。
- **Godot 重制版**（Godot 4.6 + GDScript，`godot/` 目录）：**当前活跃开发线**。路线 B = 真 3D 几何 + 复古呈现层（240×135 SubViewport、最近邻过滤、距离雾、billboard 精灵）。

数据同源：两版本共用 `assets/config/*.json` 数值配置与 LevelData 地图数据。

## 当前状态快照

| 维度 | 状态 |
| --- | --- |
| 活跃开发线 | Godot 重制版（`godot/`） |
| 分支策略 | `master`=有效主线；`dev1`/`dev2`=功能开发+验证分支，**用户实机验收合格后方可合入 master** |
| **当前任务** | **双线首批（A 批 2 + B 批 1 + 热修）已验收合入 master（2026-08-30）**。当前并行开发：A 批 3 二层夹层试点（dev1，CodeBuddy）+ B 批 2 街道/建筑布局实装（dev2，CodeBuddy）——B 批 2 前置设计输入：**map2 马路地面断面设计（沥青马路+交通线/路牙石板地面沿用 Floor Tile/两侧建筑墙）由守门方设计交底**，见 `docs/plans/2026-08-30-B线-map2马路地面断面设计.md` |
| 最新提交 | `master` = `dev1` = `dev2` = `2616d4b`（三分支对齐）。master 新含：**A 批 2 敌人 y 感知**（垂直差阈值 `detectionVerticalTolerance: 1.5` 配置驱动 + 原点基准校准 + 坡道追击实测）+ **B 批 1 跨关结算/转关 + map2 灰盒**（campaign [0,2,1] / goals 旗挪庭院 / level_ext.maps 外部地图管线 / level_end 结算态 / 场景内重建保留 Player / 命中率计数 / narrative 第二关文案）+ **验收热修**（HUD.setup_minimap 转关先 remove_child+queue_free 旧 MiniMap 防幽灵叠加；庭院东墙大门贴花 (191,31) face=W；贴花 E/W 面沿 y 延伸）+ A/B 线规划文档入库。此前已含：P2a 全套 + 混合路线规划 + 阶段一 + AE-219 敌人批次 + EDAA 文案批 + 正名/徽记/口径批 + HUD 观感批 + 出生点货运大门 + README 刷新 + P2b 第一批地形机制 + 大门贴花热修 |
| 游戏完成度 | 双地图可完整通关（找信标撤离），菜单/简报/HUD/音效齐备；HUD 右上角实时小地图 |
| 武器系统 | 手枪（半自动 20 发，初始持有）+ 冲锋枪（全自动 30 发，场景拾取）+ 霰弹枪（泵动 8 发、6 弹丸×20，北部仓库拾取）；手枪/冲锋枪共享普通备弹，霰弹枪独立使用 12 号霰弹；HUD 弹药行显示口径（手枪/冲锋枪 9×19mm、霰弹枪 12号霰弹，`weapons.json` `ammoType` 配置驱动） |
| 武器升级 | 通用组件一/二/三阶：手枪 20→25→30→33（三阶全自动）/ 冲锋枪 30→40→50→60；霰弹枪弹匣 8→12→16→24、弹丸 6→7→8→10、单丸伤害 20→25→30→40，二阶转半自动、三阶转低速全自动 |
| 防护服 | 电池拾取 +10 能量（上限 100），**50% 减伤**（`armorAbsorbRate` 配置化，1 能量抵消 1 点被吸收伤害）；a/w 符号位 + 每第 4 个 H 符号位改生电池（map0 电池 37/急救包 82，map1 17/34） |
| 剧情背景 | **正式标题「静默之前」（2026-08-29 用户四选一选定，《Snuffers》前传定位）**。**世界观转移主体完成**：甄别处二级探员「提灯」2026 年秋先行渗透城区仓库区（外星寄生孢子 AE-219 最早坠落点）；敌人三型 = 外星寄生孢子囊/寄生人类宿主/巨型突变体宿主（设定源 `E:\EDAA`）。narrative.json 已全面 EDAA 化（简报/胜负/拾取描述/敌名/区域提示），世界观文案 100% 集中于此单文件 |
| 切换绑定 | Q / 鼠标滚轮循环切换（事件驱动，经 `Main._unhandled_input`） |
| 时停机制 | Viewport 显式 PROCESS_MODE_PAUSABLE（Main 为 ALWAYS 处理 UI/输入，勿让游戏实体逃出 Viewport 子树） |
| 回归测试 | SmokeTest **131 项全绿**（三分支对齐 `2616d4b` 后统一口径；构成：109 基准 + A 批 2 +6 + B 批 1 +14 + 验收热修 +2）：`godot --path godot --headless res://scenes/tests/SmokeTest.tscn`（调度挂物理帧，裸命令稳定复现；近期新增覆盖：A 批 2 敌人垂直阈值·隔层不侦测·坡道追击实测 / B 批 1 campaign 序列·goals 旗挪庭院·转关重建·跨关保留·命中率计数 / 热修：转关后小地图唯一性·庭院东墙大门贴花墙格落位(face=W 沿 y 延伸)） |
| 用户验收 | 2026-08-29 **用户批准合入 master**（HUD 右侧组并排/霰弹枪手持重制/室内布景道具/简报换行修复 + 出生点货运大门）——此前实机反馈两项已修复：①霰弹枪模型"没应用"=导入缓存陈旧（源贴图更新未 --import，游戏用旧 .godot 缓存）；②简报文字出界=限宽 720 + 自动换行。2026-08-29 AE-219 敌人批次用户实机评"整体可以"；阶段一（外立面+庭院道具）用户确认可用；2026-08-28 P2a 验收通过并合入 master。2026-08-29 用户验收 P2b 地形批时发现**大门贴花悬浮空中未贴墙**→守门方窗口探针实证根因（配置行号偏移一行）并热修（c672b61），复验通过后与地形批一并合入 master（0369eaf） |

## 最新进展（新 → 旧）

- **2026-08-30** — **双线首批 + 验收热修合入 master（用户实机验收通过，三分支对齐 `2616d4b`，回归 131/131 全绿）**。用户 2026-08-30 上午实机验收（临时出生点补丁 (164,32) 就近看门洞/庭院/转关，用后已撤）：A 批 2 + B 批 1 通过，反馈两条均当批热修——**①转关后 map2 小地图透出第一关迷宫残影**：根因=`HUD.setup_minimap` 每次新建 MiniMap 节点不释放旧实例，半透明底板（α=0.55）下旧图幽灵显现；修复=先 `remove_child`+`queue_free` 旧节点再建新（**坑：queue_free 延迟生效，同名节点仍在树会致新节点被自动改名 MiniMap2**，必须先摘除）；SmokeTest +1 断言（转关后 MiniMap 唯一）。**②第一关终点旗正前方东墙加卷帘大门贴花**（叙事：第二关=穿过此门入街）：`_build_wall_decals` 扩展 E/W 面沿 y 延伸 w 格（原仅 N/S 沿 x），level_ext 新增 (191,31) w2 face=W 覆盖格对照围墙环核验；SmokeTest 贴花墙格断言同步支持 E/W 口径（123→125）。合并：master ff dev1（8912d37）→ merge dev2（a4254a7），SmokeTest.gd 唯一冲突按并集解决（帧号 16 双线撞车，B 线 campaign 配置测试挪帧 17），合并后回归 131/131。**推送曾因 GitHub 断连（443 超时）受阻，用户挂代理后已于当日补推核实（master/dev1 = `ab76dde`，dev2 = `fb2ca39` 含 B 批 2 前置马路素材与设计交底）**。下一步：A 批 3 / B 批 2 开工；B 批 2 前置 = 守门方 map2 马路地面断面设计交底（用户 2026-08-30 指令：沥青马路+交通线 / 两侧路牙石板地面沿用 Floor Tile / 再往边建筑墙）
- **2026-08-29** — **双线首批开发完成 + 守门方复审通过（待用户实机验收）；守门方角色 QoderWork → Kimi 交接完成**。**A 批 2 敌人 y 感知（dev1 `a4328e4`，3 文件 +98/-1，回归 109→115 全绿）**：enemies.json 三模板 +`detectionVerticalTolerance: 1.5`；Enemy.gd 侦测分支加垂直差过滤（if/elif 首门：超阈值不侦测/不追击/不攻击，隔层开火一并阻断）+ 原点基准校准（敌原点在脚底/玩家原点在胶囊中心 0.65m，校准后阈值语义=纯地形高差：平台 1.3m 互侦测防站台白嫖、层间 2.6m 隔断）；SmokeTest +6（含坡底放敌 50 帧沿坡上升 y=0.64m 实测/构造高差 3.2m 隔层不侦测/同层回归）。**B 批 1 跨关卡结算/转关 + map2 灰盒（dev2 `4f16cfa`，9 文件 +337/-14，回归 →123 全绿）**：level_ext 三新段（campaign sequence [0,2,1] / goals 第一关旗挪庭院 (179,32) + suppressDataFlag 抑制室内 F 旗 / maps map2 灰盒 140×84：围墙环+西端出生+东北信标+敌 3 拾取 3）；LevelGenerator 外部地图管线（`_ext_map_cfg` 优先命中，LevelData 红线零触碰）+ 地图尺寸元数据化 + outdoor 无天花板 + 转关复用实例的 wall_cells 重置（首跑暴露 4546 格 bug 已修）；Main `level_end` 结算态（Enter 进下一关/R 重打/ESC 回菜单）+ `_advance_level` 场景内重建（保留 Player 免序列化）+ 每关统计快照；命中率 = hits/shots（击发即计数，霰弹一发=一次）；narrative 第二关简报/胜利文案（"从不同时入睡"前传彩蛋衔接正传《回南》）+ 第一关收尾改庭院口径。**守门方复审（Kimi）**：双批回归复跑全绿（dev1 115/115、dev2 123/123）+ diff 抽审通过——零逻辑侵入不变式「墙碰撞体数==墙格数」动态断言保持、LevelData.gd diff=0 实证零触碰、level_ext 冲突段纪律合规（B 线未碰 A 线 terrain 段）。**角色交接**：守门方 QoderWork → Kimi（交接文档副本 `守门方角色交接-QoderW.txt` 在仓库根，untracked 备查）；9 项决策点 CodeBuddy 按默认提案执行（用户未逐项回复，口径见快照「当前任务」行）。**待清理（待用户点头）**：untracked 临时文件 `cmsg_b1.txt`（CodeBuddy 删除未遂托守门方清理）+ 守门方复审日志 `smoke_dev1.log`/`smoke_dev2.log`。**实机验收点**：A 批 2 = 庭院土丘上下敌我互动（站平台顶地面敌沿坡追上；<1.5m 低台互侦测属预期设计）；B 批 1 = 第一关打到庭院旗 → 弹结算（击杀/命中率/拾取/用时）→ Enter 落地 map2 灰盒装备全保留弹匣补满，R/ESC 仍可用。下批计划：A 批 3 二层夹层试点（dev1）/ B 批 2 街道建筑实装（dev2），无依赖可并行
- **2026-08-29** — **B 线规划文档到位：《map2 室外第二关规划》（CodeBuddy，dev2 `26a8e9e`，待用户拍板）**。守门方复审通过（代码勘察结论抽查属实：`map_index` 无赋值路径 → map1 当前不可达、`_stats_text` 存在、briefings 现有 2 条需补位）。三个架构决策（均附默认提案）：①map2 数据走 `level_ext.maps` 配置段（`LevelData.gd` 零触碰，配置层一贯性）；②campaign 序列 **[map0 仓库区 → map2 街道 → map1 地下设施]**——既有 map1 简报文案正好当终点关，叙事闭环资产零浪费；③跨关全保留 + 弹匣自动补满（场景内重建转关、保留 Player 实例，免序列化）。批 1 = goals 旗挪庭院 + `level_end` 结算态（新增命中率计数）+ 转关流程 + map2 灰盒；布局定稿 140×84 主街东西轴 + 北侧可进入建筑 ×3（转运站/便利店/派出所）+ 南侧装饰立面体块群（零侵入）+ 东北信标土丘高点；地形 T1+T2+T5 本阶段、T3 后置。文案含前传彩蛋（"从不同时入睡"衔接正传）与转运站伏笔（衔接 map1 简报）。**决策点五项待拍板**：①map1 去向（默认顺延第三关）②跨关保留（默认全保留）③第一关终点（默认旗挪庭院）④map2 尺寸骨架（默认 140×84）⑤结算口径（默认每关独立 + 最终关累计）。与 A 线合计 9 项待拍板
- **2026-08-29** — **A 线规划文档到位：《室内可玩多层（二楼/三楼）实施方案》（CodeBuddy，dev1 `8c326d8`，待用户拍板）**。守门方复审通过：layers 层叠数据模型（每层独立符号网格/上层为下层子集自然退台/`slabHoles` 楼板挖洞/实体绝对坐标段）、敌人 y 感知阈值 `detectionVerticalTolerance`（批 2 前置，`enemies.json` 配置驱动）、坡道复用 P2b 地形工厂（platform/ramp 加 `floor` 字段）、小地图楼层化（分层纹理+当前楼层判定）；无 `layers` 配置 = 零变化、一楼 4197 不变式原样保留。批次表：批 2 敌人 y 感知 → 批 3 二层夹层试点（东侧仓库上方）→ 批 4 三楼退台（顶板 7.8m 与装饰外立面精确对齐）→ 批 5 小地图楼层化。**决策点四项待拍板**：①夹层选址（默认东侧仓库）②二层敌人密度（默认 1–2 固定哨戒）③楼梯状坡道贴图（可省）④三楼露台（默认做）。B 线规划文档同日交付（见上条）
- **2026-08-29** — **P2b 第一批地形机制 + 大门贴花热修合入 master（用户验收通过，三分支对齐）**。用户实机验收庭院地形（平台+斜坡）通过、大门贴墙修复复验通过 → dev1（d823765 地形批 + c672b61 热修 + 0369eaf STATUS）ff 合入 master，dev2 同步，三分支对齐 `0369eaf` 并推送（其间 GitHub 网络抖动约半小时，后台轮询恢复后补推成功）。合并后回归 109/109 全绿。下一阶段双线：A 线室内可玩二/三楼（层叠数据模型 + 敌人 y 感知/地面吸附 + 小地图楼层化）、B 线第二关室外 map2（街道/建筑/多地形 + 跨关卡结算转关流程）——CodeBuddy 先出下一阶段规划文档，用户拍板后分批开发
- **2026-08-29** — **出生点货运大门贴花悬浮热修（用户实机反馈，守门方直接执行，dev1 c672b61，已随本批合入）**。用户验收截图指出大门贴图悬浮空中未贴墙。窗口探针实证根因：配置 `wallDecals y=37` 是空地板格（`wall_cell(4,37)=false`），quad 立于 z=73.95 空地，距真墙面（row 38 北面 z=76.0）2 米——MAPS 三引号串以换行开头，`split` 后 rows[0] 为空行，当初按源码行号读图整体偏移一行；原批次仅断言贴花数量、落位漏检。修复 = y 37→38（贴花贴真墙，5cm 防共面 offset，窗口探针截图复检贴墙无缝）+ SmokeTest 新增「贴花覆盖格必须在 wall_cells 中」不变式（108→109）防复发。回归 109/109 全绿。教训入交接须知：level_ext 坐标落位必须对照 wall_cells 核验（防 rows[0] 空行偏移）
- **2026-08-29** — **P2b 第一批：高度地形机制（平台+斜坡）落地庭院（A 线，CodeBuddy 双线协作首批，dev1 d823765，待用户实机验收）**。CodeBuddy 执行、守门方复审通过（回归复跑 108/108 全绿 + diff 抽审）。`level_ext.json` 新增 terrain 段：庭院西北角平台（3×4 格，顶面 1.3m）+ 东向斜坡（3×2 格，坡度 12.2°，暖土色 tint 区分庭院冷调）；`LevelGenerator._build_terrain` 通用地形体工厂（BoxMesh+BoxShape 同源；斜坡按低/高端点构造正交 Basis 贴合坡面，两端各延 5cm 咬合地面/平台消缝；坡度≥45° 或高度越界容错跳过）。**零逻辑侵入不变式延续**：玩法元素进碰撞但不登记 wall_cells/buckets，独立 StaticBody3D 挂 terrain 组 → 敌人 AI/小地图/墙碰撞计数（4197==4197）零改动。SmokeTest +5 断言（103→108：地形体数量与配置一致/坡度可行走/平台顶面高度射线精确/玩家实走斜坡登顶 1.30m/立于平台范围内）。落位庭院理由：无敌人，避免敌人 y 感知批次前暴露隔层侦测；兼作 T2 阶梯式废墟土丘预演。推送因网络抖动由守门方补做。下一步：A 线批 2 敌人 y 感知/地面吸附；B 线批 1 map2 室外布局规划文档（先过用户拍板）
- **2026-08-29** — **霰弹枪迭代原稿归档入库 + README 批/归档批合入 master（用户批准）**。撤销同日 README 批加入的 `assets-src/**/*_raw.png` 忽略规则，霰弹枪视图模型 10 张 AI 迭代原稿（`assets-src/shotgun/viewmodel*_raw.png`）正式归档入库——从原稿到定稿（双手持握/下插直弹匣）的迭代轨迹完整可追溯，定稿管线脚本 `process_viewmodel_r2.py` 同目录在档。README 全面刷新批（826ff4b）与本批一并 ff 合入 master 并推送；dev2 同步至与 dev1 相同进度。纯资产+文档零逻辑改动，回归 103/103 保持全绿
- **2026-08-29** — **README 全面刷新 + 工作区清理（dev1，纯文档零逻辑）**。README 对齐 EDAA 世界观现状：关卡名改「城区仓库区 / 地下隐匿设施」，敌人表换三型敌名（外星寄生孢子囊/寄生人类宿主/巨型突变体宿主，生命 80/100/300 不变），测试数 68→103（覆盖描述补夜空庭院/外立面/贴花/世界观断言），项目结构补 `assets-src/`；`.gitignore` 新增 `__pycache__/`（`*_raw.png` 忽略规则同批加入后即被归档批撤销，见下条）。回归 103/103 保持全绿
- **2026-08-29** — **项目全面更名：Signal Lost → 静默之前（Before the Silence）**（用户批准）。正名批后用户拍板全面统一身份：①本地文件夹 `signal-lost` → `before-the-silence`（提交后重命名）；②Godot `config/name` → 「静默之前」（窗口标题同步；主菜单标题此前已是）；③README 标题与开头换名并顺带把过时的诺亚/2057 引言改为 EDAA 世界观（正文敌人表/关卡名等仍旧，**README 全面刷新另立一批**）；④CLAUDE.md/PROJECT.md/两份规划文档/启动 bat/gen_gate.py 路径引用同步。**GitHub 仓库改名已完成**（用户当日操作：Nagi-226/before-the-silence；本地 `git remote` 已同步并推送验证，旧址自动重定向）。纯更名零逻辑改动，回归 103/103 保持全绿
- **2026-08-29** — **积压批次 + 出生点货运大门合入 master（用户批准）**。两批一并落地：**①积压观感/布景批**——HUD 底栏右侧组（防护/生命/金币）改右锚并排（+双布局不变式）；霰弹枪手持模型重制定稿（黑聚合物+下插直弹匣+双手持握，108×144 底对齐，**武器数值零改动**，管线脚本 `assets-src/shotgun/process_viewmodel_r2.py` 入档可复现）；室内布景道具 20 个（箱子/碎石，`level_ext.props`，均为已验证室内地面格）；简报文字出界修复（内容限宽 720 居中 + `AUTOWRAP_WORD_SMART` 换行）；**导入缓存坑根治**——霰弹枪模型"没应用"实为源贴图更新后未 `--import`，游戏仍用 `.godot` 旧缓存，重导后显现（回归 101→102）。**②出生点货运大门**——程序化生成卷帘门贴图 `Gate Cargo.bmp`（128×64 与墙同像素密度：帘板/警示条/锈迹/底缝未关严/橙色状态灯，脚本 `assets-src/gate/gen_gate.py` 可复现）；新增**墙面贴花通用机制** `level_ext.wallDecals` + `LevelGenerator._build_wall_decals`（QuadMesh 贴墙面、按 face 朝向、offset 防共面，**不进 wall_cells/碰撞/AI/小地图**）；大门挂在出生点背后墙（row38/cols4-5/朝北——原批误配 row37 致悬浮 2 米，c672b61 已修正），配 `narrative.json` 潜入点提示，为提灯入仓提供叙事合理化。回归 102→103 全绿（新增贴花数量断言；布景零逻辑侵入不变式 4197==4197 保持），探针截图自检通过。用户同期提出下一阶段方向（待规划）：室内二/三楼可玩层+小地图楼层化、第二关室外关卡（到庭院→第一关结算→进第二关）、室外地形/街道/建筑规划
- **2026-08-29** — **正名+观感批落地（dev1，已随本批合入 master）**。用户验收 EDAA 文案批时三项指令：①游戏正名——四候选（信号中断/提灯/静默之前/落点）用户选定**「静默之前」**（主菜单标题，副标题改「《Snuffers》前传 · RETRO FPS」；窗口/项目英文名 Signal Lost 不动，前传身份=熄烛者进场静默清剿之前）；②菜单徽记背景由 `STRETCH_KEEP_ASPECT_COVERED`（撑满裁切出界）改 **`KEEP_ASPECT_CENTERED`**（任意分辨率完整容下居中，两侧留黑边）；③HUD 下半区弹药行增显口径：`weapons.json` 三武器新增 `ammoType`（手枪/冲锋枪 9×19mm、霰弹枪 12号霰弹）→ Player `weapon_changed` 信号扩第三参数 + `weapon_ammo_type()` 访问器 → HUD 弹药行格式「武器 · 口径  弹药 X/Y」，AmmoLabel 放宽至 320px。SmokeTest +4 断言（徽记容下模式/标题文本/手枪与霰弹枪口径显示），**回归 96→100 全绿**；窗口实证：菜单徽记完整居中标题可见、局内弹药行「手枪 · 9×19mm」确认。**用户实机截图反馈弹药行与生命行文字重叠**（口径文案加宽至 ~320px 侵入原生命行 x=240 起始区）→ 生命/防护行右移至 x=360 让位 + SmokeTest 增「弹药行右缘 ≤ 生命行左缘」布局不变式，**回归 100→101 全绿**
- **2026-08-29** — **narrative.json 文案 EDAA 化完成（dev1，待实机验收）**。用户对 AE-219 批次实机评"**整体可以**"，并下达四项文案/美术指令，本批全部落地：①敌名三型按用户指定更名为**外星寄生孢子囊/寄生人类宿主/巨型突变体宿主**（`narrative.json` `enemy_names`，Main.gd 消费链零改动）；②开场剧情全面对齐 **EDAA 世界观·当代 2026 年秋**（非旧 2057 线）——简报两章改写为「提灯」甄别处二级探员渗透仓库区（瞭望阵列侦测/熄烛者特遣队待命/提灯必至但不能折）+ 地下设施章（包装转运痕迹/巨型突变体警告），胜负结算衔接熄烛-01「雷暴」进场清剿前传钩子，拾取描述与区域提示同步换叙事口吻（「萤」提示/灯芯失联遗落霰弹枪）；③主菜单背景换 **EDAA 徽记**（`E:\EDAA\EDAA徽记.png` → `MenuBG_EDAA.png`，遮罩透明度 0.45→0.35 透徽记）；④室内仓库主墙（X 符号，map0 4140 格）砖墙→**混凝土墙**（AI 生成 3×3 瓦片原稿 → ffmpeg `crop=960:960:0:0,scale=64:64:flags=neighbor` 64×64 BMP，可复现脚本 `assets-src/walls/process_walls.sh`；map1 石墙不动）。**回归 90→96 全绿**（新增 6 断言：提灯/2026 在场、2057/诺亚零残留、熄烛钩子、敌名三型、Walls_X 混凝土贴图、徽记背景可加载），窗口实证三帧截图（菜单徽记/简报文案/局内混凝土墙）均通过。**世界观文案自此 100% 集中 `narrative.json` 单文件，.gd/.tscn 零硬编码**。待实机验收：简报措辞、菜单徽记观感、混凝土墙与地砖/雾的搭配
- **2026-08-29** — **世界观转移启动 + AE-219 敌人批次落地（dev1，待实机验收）**。用户拍板：本游戏 = 《Snuffers》（熄烛者 01 队雷暴重装清剿）的**前传**——二级轻装探员「提灯」先行渗透侦查城区仓库区（外星寄生孢子 AE-219 最早坠落点），设定源 `E:\EDAA` + Snuffers concept-art 三张。**素材管线**（延续阶段一 AI 生成→抠像路线）：ImageGen 2x2 格 1024px 原稿 ×4 → 背景非纯品红按图四角采样定键色（BE126C/CA1391/B70E6D/CF0099，sim 0.25）→ ffmpeg 裁格抠像 → PowerShell(System.Drawing) 修边+最近邻降采样 → **10 个成品**（三敌人高 64px：孢子囊/人类宿主/巨型宿主各基础+攻击帧，宿主系双攻击帧=近战爪击+喷/掷孢子；蓝绿孢子投射物小 24px/大 40px），0 品红残留，原稿归档 `assets-src/ae219_enemies/`。**接线 4 处**：`enemies.json` 三模板切换（新增 `attackMode`/`meleeRange`/`meleeDamage`/`meleeInterval`/`projectileSprite`/`projectilePixelSize` 字段；巨型 moveSpeed 2.5→3.5 全场最快）；`Enemy.gd` 双攻击帧加载（` Attack Melee.png`/` Attack Spore.png`，缺失优雅降级旧式 ` Attack.png`）+ 贴身爪击模式（`meleeRange<=0` 即纯远程，行为与旧版完全一致）+ `PIXEL_SIZES` 减半（64px 精灵保世界高度 1.44/1.84/2.4m 不变）；`Projectile.tscn/.gd` 敌人弹道孢子广告牌化（SporeSprite 节点，玩家弹丸沿用球形网格）；SmokeTest +9 断言。**回归 81→90 全绿**（首轮巨型宿主双帧 FAIL：攻击帧文件名缺 "Host" 基名前缀，重命名对齐后通过）。阶段一同期验收通过（用户确认庭院+室外街道道具可用），临时出生点补丁已撤。**用户实机验收评"整体可以"**，敌名按指定三型更名（已并入上一批）
- **2026-08-28** — **混合路线阶段一落地（dev1，待实机验收）：装饰外立面 + 庭院道具**。素材稿 6 张用户过目批准（2 立面贴图 + 4 道具精灵，AI 生成→ffmpeg 抠像管线可复现脚本在 `assets-src/facade_props/process_facade_props.sh`）。落地 4 处：`assets/config/level_ext.json` 新增 `facade` 段（col167/rows26-38 上下各延 1 格，现有墙顶 2.6m 之上叠 2 层×2.6m 窗户带 + 女儿墙，总高 7.8m ≈ 三层体量；`uvScaleX=3.8` 横向平铺保窗户近方形）+ `props` 段（5 个庭院道具：瓦砾×1/木箱×2/路灯×1/枯树×1）；`LevelGenerator` 新增 `_build_facade`（QuadMesh 竖面朝 +X 庭院侧，与原墙东面留 0.05m 防共面闪烁）+ `_spawn_props`（Sprite3D billboard/NEAREST，扫描贴图最低不透明像素行自动落地）；SmokeTest 新增 4 断言（外立面生成/顶高 7.80m/道具 5 个/碰撞体数==墙格数 4197==4197）。**零逻辑侵入不变式实证**：立面与道具均不进 `wall_cells`/buckets/碰撞 → 回归 81/81 全绿。实机调参备选：窗户比例、雾与立面观感、门洞框景遮挡、道具落位
- **2026-08-28** — **P2a 验收通过，合入 master + 回归竞态根治**。用户 2026-08-26 实机试玩认可室外风格、门洞出生点视角复核后，2026-08-28 正式宣布"通过验收"——P2a 全套（庭院外扩/门洞打通/夜空环境/小地图跟随）+ 混合路线规划合入 `master`。合并前回归曾连挂"子弹命中敌人"项 2 次，诊断定性为**测试调度竞态而非代码回归**（工作区与曾通过提交零差异；对照实验 `--fixed-fps 60` 锁帧后 77/77 全过）：SmokeTest 帧号原挂 `_process` 渲染帧，headless 下渲染不受垂直同步约束，机器快时渲染帧领先物理步，校验在弹丸飞到前执行。修复 = 调度循环改挂 `_physics_process`（帧号对齐 60Hz 物理时钟），裸命令两轮复跑 77/77 全绿。**下一步：混合路线阶段一**（装饰外立面 + 庭院道具），素材稿生成中待用户过目
- **2026-08-26 规划定稿（代码未动工）** — **外景拓展与多层建筑 · 混合路线**。用户实机反馈：P2a 室外区整体风格可以，但出门回头只见单层平房太突兀——回头应见至少三层仓库类建筑；同时要求室外继续拓展更丰富地形。**拍板决策：混合路线**——①P2b 之前先做**方案 A**：装饰外立面（现有墙顶之上叠 2 层 ≈5.2m 带窗面板 + 女儿墙，总高 ≈7.8m 三层体量，不进 `wall_cells`/buckets → 碰撞/AI/子弹/小地图零改动）+ 庭院 billboard 道具（Sprite3D 同源技术，首批不加碰撞），立刻兑现视觉说服力、零玩法风险；②P2b 斜坡/楼梯落地后引入**层叠数据模型**（`level_ext.json` layers 段，上层网格为下层子集自然退台），从东侧仓库上方一小块可玩二层夹层做起（压在门洞回头视线里）；③三层留给装饰性天桥/半开放走廊。室外地形 T1-T5 并行路线：道具 → 阶梯式地形起伏（复用 P2b 高度系统）→ P2c 暗洞/河/桥 → 大型室外区（map0 外扩或新建 map2，验收后拍板）→ 雾/天空按图差异化。**架构勘察结论**（代码核实）：单层符号图是"平房"根源（DOOM 式一格一态），敌人侦测为水平投影忽略 y、玩家无跳跃键 ≤45° 坡可行、子弹 3D 飞行天然兼容楼板遮挡、小地图需引入当前楼层概念——可玩多层必须排在 P2b 之后。完整蓝图（含阶段一文件清单/配置 schema/验收点）：`docs/plans/2026-08-26-外景拓展与多层建筑混合路线规划.md`
- **2026-08-26** `f6bbcc6` — **P2a 夜空室外区实施完成（dev1，待实机验收）**：7 文件落地蓝图（`level_ext.json` 新建 + 6 修改），回归 77/77 全绿。庭院自 map0 东墙外扩（x167-191/y26-38），门洞 col167×rows32-33；夜空 Environment（ProceduralSkyMaterial 深夜蓝黑）+ 雾密度 0.012→0.0045；P1.5 小地图跟随模式（玩家居中钳制+视窗裁剪绘制）。**审查修复致命缺陷**：门洞格在原图中本是东边界 X 墙，OX-Alpha 原实现只登记露天格未移除原墙，导致门在逻辑/物理/小地图三层都打不开（回归 2 项失败实证）；修复 = 门洞格先从 `wall_cells`+buckets erase 原墙，围墙环与原图已有墙重叠格跳过避免重复网格/碰撞。新增门洞物理射线通行断言。待实机验证观感：距离雾与夜空兼容度、庭院内墙顶面仰视、门洞框视觉
- **规划定稿** — **P2a 夜空室外区：启动条件核查 + 实施蓝图**。用户四条决策：①室外区数据走**方案 A**（JSON 配置段，不动 LevelData.gd 与 gen_level_data.py）；②按规划原文"**向外扩出庭院**"物理扩尺寸——室内迷宫墙上开一道门通往室外，而非在原图内挖天花板洞；③**P1.5 小地图跟随模式随本批一起做**；④铁律：**只在 dev1 开发 → 用户实机体验 → 明确批准后才可合入 master，禁止擅自合并**。
  - **Git 前置已完成**：dev1 已从 `93a1cc8` 快进同步到 master 的 `85e07ee`，无分叉。
  - **选址实测（基于字符图逐格核实 + BFS）**：东墙门洞 col 167 × rows 32–33（2 格宽；出生点 P(4,34) 经 BFS 验证可达该门洞，门不会开进隔离区；紧邻南侧小房间顶墙在 row 34，互不干扰）；庭院外扩矩形 **x:168→191, y:26→38（24×13 格 ≈ 48m×26m）**——原地图串止于 col 167，以东全空白，外扩零冲突。
  - **关键架构发现**：庭院位于原 168×68 地图矩形**之外** → 原整片天花板平面无需挖洞！室内屋顶自然延伸到门洞正上方形成"屋檐边缘"，穿门而出即露天，「室内→室外」过渡免费获得。
  - **实施清单（7 个文件，其中 2 新建）**：🆕 `assets/config/level_ext.json`（庭院矩形/门洞坐标/围墙符号石墙 S 风/室外地面材质/小地图跟随开关）；`scripts/autoload/GameData.gd`（加 `level_ext_cfg` 加载，沿用 `_load_json` 模式）；`scripts/levels/LevelGenerator.gd`（核心：`_apply_courtyard()` 向 buckets 追加围墙格+登记门洞为地板；**`_build_walls` 的 MultiMesh 包围盒必须改为按实际格子计算，否则原地图外的围墙会被包围盒剔除不显示**；地面延伸覆盖庭院并用暗色苔原配色区分室内地砖；天花板不动）；`scenes/main/Main.tscn`（Environment `background_mode=2` + ProceduralSkyMaterial 深夜蓝黑渐变夜空；雾密度调低防室外糊成灰幕）；`scripts/ui/MiniMap.gd`（P1.5 跟随：地图超视窗时启用，玩家居中+贴边钳制，`draw_texture_rect_region` 视窗裁剪绘制）；`assets/config/narrative.json`（门洞发现 toast「东侧墙体破开一道裂口，夜风灌入……」）；`scripts/tests/SmokeTest.gd`（新增断言：天空模式生效/围墙生成/门洞已开 `is_wall(167,32)==false`/庭院角墙存在/小地图跟随激活）。
  - **自动生效无需改动（已逐一代码核实）**：围墙碰撞体（`_build_collision` 遍历 buckets）、子弹被新墙阻挡、敌人 AI 视线与寻路（`wall_cells` 自动含新墙）、无限地面碰撞、小地图尺寸自适应（按最远墙格计算）、敌人红点在庭院内定位。全材质 UNSHADED 无光源依赖，切夜空不打乱现有渲染。
  - **已知风险（规划文档预判）**：现有距离雾参数（fog_light_color 0.08/0.09/0.12 + density 0.012）为封闭走廊设计，室外看夜空可能糊成灰蓝，需实机微调；墙顶面（BoxMesh 高 2.6m，unshaded）从室外仰视观感需实机确认。
  - **收尾项**：删除根目录临时勘察脚本 `tools/p2a_survey_tmp.py`（BFS 选址一次性工具，用后即删）。回归命令 `godot --path godot --headless res://scenes/tests/SmokeTest.tscn`（Godot 经 WinGet 安装，真实进程名 `Godot_v4.6.2-stable_win64`；若 PATH 无 godot 命令先探测，找不到再向用户要路径）。
- **2026-08-11** — **M3/P3 表现补齐：第三武器霰弹枪 + 12 号霰弹**（dev1 实机验收后合入 master）。新增泵动式弹匣霰弹枪：基础 8 发、每枪 6 弹丸、单丸伤害 20、散布随射程增大；通用组件逐阶提升为 12/16/24 发、7/8/10 弹丸、25/30/40 单丸伤害，二阶转半自动并取消泵动，三阶转 3.0 低速全自动。第一关拾取点位于北部仓库，双图配置生成独立 12 号霰弹补给。第一人称素材按现有手枪/冲锋枪硬边像素风对齐，枪口居中、右手握把、左手握泵，最终修复左臂裁切并使用底部对齐的专用 HUD 显示框；高清源与处理脚本已纳入版本库
- **2026-08-09** — **M3 敌人受击/死亡表现 + 双武器动作**（dev1，用户实机验收通过）。敌人增加受击白闪、上抬挤压回弹，以及倒地压扁→尸体停留→闪烁→淡出的死亡状态机；死亡后立即关闭碰撞与攻击。手枪/冲锋枪基于现有正式视图模型增加不同的射击后坐和换弹轨迹、换弹状态提示，并遵守暂停体系。死亡时序进入 `enemies.json` 配置；逐帧手部美术保留为后续可替换资源位；SmokeTest 扩至 53 断言，覆盖死亡碰撞/完整清理与武器动画启动
- **2026-08-09** — **结算时停修复 + 防护服 50% 减伤 + 道具平衡**（dev1，验收后合入 master）。根因修复：Main 为 PROCESS_MODE_ALWAYS 致整个 Viewport 子树（玩家/敌人/子弹）继承永不暂停——Viewport 显式改 PAUSABLE，一行覆盖结算/暂停菜单/简报三场景（暂停菜单期间敌人其实在动的隐患一并消除）；防护服能量改 50% 减伤（`armorAbsorbRate: 0.5` 配置化，1 能量抵消 1 点被吸收伤害）；道具平衡：w 死符号位改生电池 + 每第 4 个 H 改生电池（map0 电池 5→37/急救包 109→82，map1 3→17/45→34）；SmokeTest 扩至 49 断言（含电池/急救包精确计数与通关后注入射击弹药不变的暂停回归）
- **2026-08-09** — **P1 小地图 + P4 剧情改写**（dev1，验收后合入 master）。HUD 右上角数据驱动俯视小地图（墙体预渲染纹理+玩家箭头/敌人/信标，显式定位+resize 跟随；踩坑：CanvasLayer 直接子节点锚点不按视口解析、符号地图实际 69 行超 HEIGHT 常量）；剧情改诺亚生物科技生物兵器失控+特遣队特工（简报/结算/区域提示/README）
- **2026-08-09** — **三阶组件 + 道具体系重构**（dev1，验收后合入 master）。组件三阶：手枪 33 发+全自动 / 冲锋枪 60 发，伤害改 36/45/55，每阶射速 +10 封顶 40（合并神经加速器职能）；二阶紫/三阶金黄素材；"a 扩展弹匣""w 神经加速器"实机移除（生成层拦截，地图数据不动），"a"符号位改生**防护服能量**（Battery.png，+10，HUD 防护条，优先承伤）；急救包/电池满额拒收；组件分散放置（map0 西→东 / map1 同）；弹药箱换用 Upgrade Ammo 10.png；修复设置滑条拖动重置最大化窗口（Settings.apply 无条件 window_set_mode）；SmokeTest 45 断言
- **2026-08-09** — **通用武器升级组件（一/二级）+ 精英变异体改名**（dev1 开发，用户实机验收后合入 master）。weapons.json 新增 `upgradeComponents.levels/spawns` 配置驱动；顺序约束（越级拾取拦截+toast，拾取物保留）；`enemy_names` 变异体小BOSS→精英变异体；SmokeTest 24→33 断言并改物理帧驱动（headless 渲染/物理帧不同步踩坑）；README 武器/拾取物表同步
- **2026-08-08** `9b4acf4` — 新增 STATUS.md 进度追踪体系（本文件），CLAUDE.md / README.md 加入口指引；此前 `399b3ae` 新增 README（门面文档）、项目更名 Signal Lost（原 Retro FPS），仓库首次推送 GitHub
- **2026-08-08** `47ac39a` — **Phase 3：冲锋枪场景拾取 + Q/滚轮切换**。owned 持有模型（初始仅手枪）；`pickupSpawns` 配置生成 W 拾取物（map0 @ (20,35) / map1 @ (14,10)，不动 C++ 同源地图）；`grant_weapon` 首拾补满弹匣+自动切换，重复拾取转 30 备弹；移除 1/2 键；拾取精灵 48×41 金色光晕版；SmokeTest 扩至 24 断言
- **2026-08-08** `aa6ce5d` — 双击启动器 `启动Godot版.bat`（自动定位 WinGet 安装的 Godot 真实 exe）
- **2026-08** `5c3b3ce` — **Phase 2：双武器系统 + 数值重构 + 敌人攻击帧**。手枪半自动/冲锋枪全自动；血量 100→升级 120，敌血 80/100/300，伤害 32，敌伤×5；敌人开火切持枪攻击贴图
- **2026-08** `bfbc494` — AI 生成素材全套升级（菜单背景/墙地纹理/HUD/敌人拾取精灵）
- **2026-08** `ea39f5d` — 产品层补齐（菜单/简报/HUD/音效集成）
- **2026-08** `2aa1fb7` — 路线 B 原型（真 3D + 复古呈现层验证）
- **保护点** `d5a42a1` — C++ 版 v0.6.1 冻结归档，此后 C++ 侧不再演进

## 待办 / 下一步

（2026-08-30 双线首批 + 验收热修经用户实机验收合入 master，三分支对齐 `2616d4b`，回归 131/131 全绿。当前双线并行开工：A 批 3 二层夹层试点（dev1）/ B 批 2 街道建筑实装（dev2，前置=守门方马路地面断面设计交底）。每批走"开发→守门方复审→用户实机验收→批准合并"闭环。）

1. **A 线当前批：A 批 3 二层夹层试点**（dev1，CodeBuddy 开工）——layers 层叠数据模型 + 楼板/层墙生成 + 东侧仓库二层夹层 + 一层→二层坡道 + 二层哨戒敌/拾取（见 `docs/plans/2026-08-29-A线-室内可玩多层实施方案.md` §八）；后续批 4 三楼退台 → 批 5 小地图楼层化（用户 2026-08-30 重申：楼上楼下小地图按实际楼层刷新，与批 5 设计一致）
2. **B 线当前批：B 批 2 街道/建筑布局实装**（dev2，CodeBuddy 开工）——主街/次街/南巷 + B1-B3 可进入建筑 + 南侧装饰立面体块群（见 `docs/plans/2026-08-29-B线-map2室外第二关规划.md` §六）；**前置设计输入（用户 2026-08-30 指令）**：街道断面 = 中间沥青马路带交通线 / 两侧马路牙子上石板地面（沿用 Floor Tile 材质不变）/ 再往边大型建筑墙面——守门方设计交底见 `docs/plans/2026-08-30-B线-map2马路地面断面设计.md`；后续批 3 T2 阶梯地形 + 信标土丘 → 批 4 雾/天空差异化 + 文案终稿
3. ~~室外场景规划细化~~ **已并入 B 线规划文档定稿**（2026-08-29：map2 街道布局/建筑分布/多地形取舍见 `docs/plans/2026-08-29-B线-map2室外第二关规划.md` §4，dev2 分支）
4. ~~P2b 斜坡/楼梯 + 敌人地面吸附~~ **P2b 第一批（地形平台+斜坡）已合入 master（d823765，用户验收通过）**；敌人 y 感知/地面吸附 **A 批 2 已交付待验收（dev1 `a4328e4`）**；剩余：斜坡/楼梯进室内建筑（A 批 3 起，见 `docs/plans/2026-08-29-A线-室内可玩多层实施方案.md`）
5. 武器拾取音效目前复用 `Energy Orb.ogg`，待用户提供专属音效偏好
6. 候选方向（待用户拍板）：更多武器/敌人种类、难度选择接入 Godot 版、存档系统、更多地图；EDAA 侧待决：2057 冲突时间线（Snuffers 主线）与本片 2026 前传的关系
7. ~~【待用户操作】GitHub 仓库改名~~ **已完成**（2026-08-29：用户改名 + 本地 `git remote` 同步 + 推送验证，身份统一全部收尾）
8. ~~**README 全面刷新**~~ **已完成**（2026-08-29：世界观/关卡名「城区仓库区/地下隐匿设施」/敌人表三型/测试数 103/项目结构补 assets-src，全部对齐 EDAA 现状）

## 交接须知（Agent 必读）

**关键路径与工具链**

- Godot 4.6.2 经 WinGet 安装，真实进程名 `Godot_v4.6.2-stable_win64`（`Get-Process godot` 会假阴性）
- 回归命令：`godot --path godot --headless res://scenes/tests/SmokeTest.tscn`（分支口径：master 109 项 / dev1 115 项 / dev2 123 项全绿，2026-08-29 守门方复跑实证）。"子弹命中敌人"一项的历史偶发失败已于 2026-08-28 定性根治：根因是测试调度挂在渲染帧（headless 渲染不受垂直同步约束、可领先物理步），改挂 `_physics_process` 后裸命令稳定复现，无需 `--fixed-fps`
- **导入缓存坑（2026-08-29 实证）**：替换源贴图后直接启动游戏**不会**重导入，仍用 `.godot` 旧缓存（霰弹枪模型"没应用"即此因）——改贴图后须先 `godot --path godot --headless --import` 再启动/回归；检测法 = 源图文件时间晚于对应 `.import` 文件即缓存陈旧。另：headless 为 dummy 渲染，SubViewport 截图探针必须用**窗口模式**跑（无 `--headless`）
- **世界观文案单点维护**：简报/胜负/拾取描述/敌名/区域提示 100% 在 `godot/assets/config/narrative.json`，`.gd`/`.tscn` 零硬编码世界观串——改文案只动这一份 JSON（C++ 归档版无此文件，不存在同源同步问题）
- C++ 版回归：`ctest --test-dir build -C Debug --output-on-failure`（127 测试）

**硬性约束**

- `godot/scripts/levels/LevelData.gd` 自动生成，**不可手改**；关卡数值改动走 `assets/config/*.json`
- **地图坐标读图坑（2026-08-29 实证）**：`LevelData.MAPS[n]` 三引号串以换行开头，`split("\n")` 后 `rows[0]` 为空行 → 按源码行号肉眼读符号图会**整体偏移一行**（大门贴花 y=37 悬浮事故根因，真墙在 row 38）。`level_ext` 坐标（贴花/道具/地形）落位必须对照 `wall_cells`/地板格核验，禁止只按源码行号填写
- 不修改 `prototypes/`、`src-legacy/`；C++ CMake target 名 `RetroFPS`（exe 名）属受保护构建产物，不动
- 提交消息以 `自检: ①②③④ 通过` 结尾（仓库既有风格）
- **用户驱动协作**：Write/Edit 前需用户确认，未经指示不提交
- **合并红线（用户明令）**：功能一律先在 `dev1` 开发并让用户实机体验，**未经用户明确批准严禁合入 master**

**已验证的技术方案（避免重复踩坑）**

- **暂停体系**：`Main`=PROCESS_MODE_ALWAYS（处理 UI/输入），`Viewport`=显式 PAUSABLE——游戏实体（玩家/敌人/子弹/拾取物）必须挂在 Viewport 子树内，挂到 Main 直下会继承 ALWAYS 导致 paused 时仍在跑（2026-08-09 结算时停 BUG 的根因）
- **MultiMesh 墙体包围盒剔除**：LevelGenerator `_build_walls` 用包围盒做剔除，向原地图矩形外追加的墙体若不同步扩大包围盒将**静默不显示**（P2a 已改为按实际墙格计算包围盒）
- **原边界墙格改造（开门洞）**：把原图中的墙格改造成通道，必须先从 `wall_cells` 与 buckets 中 erase 原墙（渲染/碰撞/小地图全从中派生，只登记新状态不移除旧状态 = 门三层都打不开）；围墙环与原图已有墙重叠的格应跳过追加，避免重复网格/碰撞体
- **多层建筑约束勘察（2026-08-26）**：敌人侦测/攻击距离为水平投影（忽略 y），玩家 CharacterBody3D 无跳跃键、≤45° 坡可行——可玩多层必须排在 P2b 斜坡/楼梯+地面吸附之后；三层**外观**可先用装饰外立面兑现（不进 `wall_cells`/buckets → 碰撞/AI/子弹/小地图零改动）；子弹为 3D 物理飞行，楼板有碰撞即天然遮挡
- **墙面贴花机制（2026-08-29）**：`level_ext.wallDecals` + `LevelGenerator._build_wall_decals`——QuadMesh 贴墙（UNSHADED + NEAREST），`face` N/S/E/W 决定法线朝向与旋转，`offset`（默认 0.05）防共面闪烁，节点挂 `wall_decals` 组；**不进 `wall_cells`/buckets/碰撞/AI/小地图**，布景零逻辑侵入不变式（碰撞体数==墙格数）保持。首个实例 = 出生点背后货运大门（卷帘门，底缝未关严 = 潜入点叙事；row38/cols4-5/朝北；「贴花覆盖格必须为真实墙格」SmokeTest 断言守护，防行号偏移悬浮）
- **地图勘察方法**：直接解析 LevelData.gd 字符地图做 BFS 连通性/选址验证即可，不必启动游戏（脚本曾存根目录 `tools/p2a_survey_tmp.py`，属一次性工具，P2a 实施时删除）
- 脉冲输入（Q/滚轮）测试：`Input.parse_input_event` 注入 + `await get_tree().physics_frame` 等帧
- 窗口实证：SubViewport `get_texture().get_image().save_png()` 拿像素级渲染，比窗口截屏可靠
- 视线验证：截图/观察前先用 `PhysicsRayQueryParameters3D` 射线确认相机与目标间无墙体遮挡（排除玩家/目标自身 RID）

**文档索引**

| 文件 | 内容 |
| --- | --- |
| `STATUS.md`（本文件） | 进度快照与交接入口 |
| `PROJECT.md` | 项目全量上下文（架构/Agent 团队/规则/钩子，C++ 时代编写） |
| `CLAUDE.md` | Agent 配置索引 + 协作协议 |
| `README.md` | 对外门面 |
| `design/` · `docs/plans/` | 设计规格与开发计划（最新：`docs/plans/2026-08-26-外景拓展与多层建筑混合路线规划.md`） |
