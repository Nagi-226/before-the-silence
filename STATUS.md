# STATUS — 开发进度实时追踪

> **本文件用途**：AI Agent 集群交接的唯一进度入口。任何 agent 接手前先读本文件，即可对齐项目定位、当前进度与下一步工作。
> **更新约定**：每完成一个开发里程碑（功能/重构/修复批次）后，在「最新进展」顶部追加一条，并同步刷新「当前状态快照」与「待办」。

**最后更新**: 2026-08-26 · 当前分支 `dev1`（代码 @ `f6bbcc6`）· **P2a 实机验收中；外景拓展与多层建筑混合路线规划定稿**（`docs/plans/2026-08-26-外景拓展与多层建筑混合路线规划.md`）· 远程 `origin/master` @ https://github.com/Nagi-226/Signal_Lost

---

## 项目定位（30 秒版）

Signal Lost 是复古 DOOM 风 FPS：2057 年侦察兵深入远东废墟感染区，穿越变异体围攻抵达撤离信标。仓库为**双引擎**结构：

- **C++ 原版**（SDL2 + C++17，raycasting 伪 3D）：功能完备、127 测试通过，现为**受保护归档**，不再加新特性。
- **Godot 重制版**（Godot 4.6 + GDScript，`godot/` 目录）：**当前活跃开发线**。路线 B = 真 3D 几何 + 复古呈现层（240×135 SubViewport、最近邻过滤、距离雾、billboard 精灵）。

数据同源：两版本共用 `assets/config/*.json` 数值配置与 LevelData 地图数据。

## 当前状态快照

| 维度 | 状态 |
| --- | --- |
| 活跃开发线 | Godot 重制版（`godot/`） |
| 分支策略 | `master`=有效主线；`dev1`/`dev2`=功能开发+验证分支，**用户实机验收合格后方可合入 master** |
| **当前任务** | **P2a 实机验收**（`dev1` @ `f6bbcc6`，用户已试玩、评价室外风格可以，正式验收待确认）；**外景拓展与多层建筑混合路线已定稿**：P2b 前先做装饰外立面+庭院道具（方案 A），P2b 后做可玩二层夹层——见 `docs/plans/2026-08-26-外景拓展与多层建筑混合路线规划.md` |
| 最新提交 | master @ `85e07ee`（未动）；`dev1` @ `f6bbcc6`（P2a 实施 + 门洞修复）+ docs 提交（本规划定稿） |
| 游戏完成度 | 双地图可完整通关（找信标撤离），菜单/简报/HUD/音效齐备；HUD 右上角实时小地图 |
| 武器系统 | 手枪（半自动 20 发，初始持有）+ 冲锋枪（全自动 30 发，场景拾取）+ 霰弹枪（泵动 8 发、6 弹丸×20，北部仓库拾取）；手枪/冲锋枪共享普通备弹，霰弹枪独立使用 12 号霰弹 |
| 武器升级 | 通用组件一/二/三阶：手枪 20→25→30→33（三阶全自动）/ 冲锋枪 30→40→50→60；霰弹枪弹匣 8→12→16→24、弹丸 6→7→8→10、单丸伤害 20→25→30→40，二阶转半自动、三阶转低速全自动 |
| 防护服 | 电池拾取 +10 能量（上限 100），**50% 减伤**（`armorAbsorbRate` 配置化，1 能量抵消 1 点被吸收伤害）；a/w 符号位 + 每第 4 个 H 符号位改生电池（map0 电池 37/急救包 82，map1 17/34） |
| 剧情背景 | 诺亚生物科技生物兵器事故 → 特遣队特工进入封锁区（narrative.json） |
| 切换绑定 | Q / 鼠标滚轮循环切换（事件驱动，经 `Main._unhandled_input`） |
| 时停机制 | Viewport 显式 PROCESS_MODE_PAUSABLE（Main 为 ALWAYS 处理 UI/输入，勿让游戏实体逃出 Viewport 子树） |
| 回归测试 | SmokeTest 77 项全绿：`godot --path godot --headless res://scenes/tests/SmokeTest.tscn` |
| 用户验收 | 2026-08-26 P2a 实机试玩：室外区整体风格获认可（"还可以"），正式验收与是否合 master 待用户确认；2026-08-11 霰弹枪玩法、北部仓库放置、第一人称素材尺寸与左臂边界已实机验收；此前 M3 敌人死亡与双武器动作、P1/P4 等批次均已验收 |

## 最新进展（新 → 旧）

- **2026-08-26 规划定稿（代码未动工）** — **外景拓展与多层建筑 · 混合路线**。用户实机反馈：P2a 室外区整体风格可以，但出门回头只见单层平房太突兀——回头应见至少三层仓库类建筑；同时要求室外继续拓展更丰富地形。**拍板决策：混合路线**——①P2b 之前先做**方案 A**：装饰外立面（现有墙顶之上叠 2 层 ≈5.2m 带窗面板 + 女儿墙，总高 ≈7.8m 三层体量，不进 `wall_cells`/buckets → 碰撞/AI/子弹/小地图零改动）+ 庭院 billboard 道具（Sprite3D 同源技术，首批不加碰撞），立刻兑现视觉说服力、零玩法风险；②P2b 斜坡/楼梯落地后引入**层叠数据模型**（`level_ext.json` layers 段，上层网格为下层子集自然退台），从东侧仓库上方一小块可玩二层夹层做起（压在门洞回头视线里）；③三层留给装饰性天桥/半开放走廊。室外地形 T1-T5 并行路线：道具 → 阶梯式地形起伏（复用 P2b 高度系统）→ P2c 暗洞/河/桥 → 大型室外区（map0 外扩或新建 map2，验收后拍板）→ 雾/天空按图差异化。**架构勘察结论**（代码核实）：单层符号图是"平房"根源（DOOM 式一格一态），敌人侦测为水平投影忽略 y、玩家无跳跃键 ≤45° 坡可行、子弹 3D 飞行天然兼容楼板遮挡、小地图需引入当前楼层概念——可玩多层必须排在 P2b 之后。完整蓝图（含阶段一文件清单/配置 schema/验收点）：`docs/plans/2026-08-26-外景拓展与多层建筑混合路线规划.md`
- **2026-08-26** `f6bbcc6` — **P2a 夜空室外区实施完成（dev1，待实机验收）**：7 文件落地蓝图（`level_ext.json` 新建 + 6 修改），回归 77/77 全绿。庭院自 map0 东墙外扩（x167-191/y26-38），门洞 col167×rows32-33；夜空 Environment（ProceduralSkyMaterial 深夜蓝黑）+ 雾密度 0.012→0.0045；P1.5 小地图跟随模式（玩家居中钳制+视窗裁剪绘制）。**审查修复致命缺陷**：门洞格在原图中本是东边界 X 墙，OX-Alpha 原实现只登记露天格未移除原墙，导致门在逻辑/物理/小地图三层都打不开（回归 2 项失败实证）；修复 = 门洞格先从 `wall_cells`+buckets erase 原墙，围墙环与原图已有墙重叠格跳过避免重复网格/碰撞。新增门洞物理射线通行断言。待实机验证观感：距离雾与夜空兼容度、庭院内墙顶面仰视、门洞框视觉
- **规划定稿** — **P2a 夜空室外区：启动条件核查 + 实施蓝图**。用户四条决策：①室外区数据走**方案 A**（JSON 配置段，不动 LevelData.gd 与 gen_level_data.py）；②按规划原文"**向外扩出庭院**"物理扩尺寸——室内迷宫墙上开一道门通往室外，而非在原图内挖天花板洞；③**P1.5 小地图跟随模式随本批一起做**；④铁律：**只在 dev1 开发 → 用户实机体验 → 明确批准后才可合入 master，禁止擅自合并**。
  - **Git 前置已完成**：dev1 已从 `93a1cc8` 快进同步到 master 的 `85e07ee`，无分叉。
  - **选址实测（基于字符图逐格核实 + BFS）**：东墙门洞 col 167 × rows 32–33（2 格宽；出生点 P(4,34) 经 BFS 验证可达该门洞，门不会开进隔离区；紧邻南侧小房间顶墙在 row 34，互不干扰）；庭院外扩矩形 **x:168→191, y:26→38（24×13 格 ≈ 48m×26m）**——原地图串止于 col 167，以东全空白，外扩零冲突。
  - **关键架构发现**：庭院位于原 168×68 地图矩形**之外** → 原整片天花板平面无需挖洞！室内屋顶自然延伸到门洞正上方形成"屋檐边缘"，穿门而出即露天，「室内→室外」过渡免费获得。
  - **实施清单（7 个文件，其中 2 新建）**：🆕 `assets/config/level_ext.json`（庭院矩形/门洞坐标/围墙符号石墙 S 风/室外地面材质/小地图跟随开关）；`scripts/autoload/GameData.gd`（加 `level_ext_cfg` 加载，沿用 `_load_json` 模式）；`scripts/levels/LevelGenerator.gd`（核心：`_apply_courtyard()` 向 buckets 追加围墙格+登记门洞为地板；**`_build_walls` 的 MultiMesh 包围盒必须改为按实际格子计算，否则原地图外的围墙会被包围盒剔除不显示**；地面延伸覆盖庭院并用暗色苔原配色区分室内地砖；天花板不动）；`scenes/main/Main.tscn`（Environment `background_mode=2` + ProceduralSkyMaterial 深夜蓝黑渐变夜空；雾密度调低防室外糊成灰幕）；`scripts/ui/MiniMap.gd`（P1.5 跟随：地图超视窗时启用，玩家居中+贴边钳制，`draw_texture_rect_region` 视窗裁剪绘制）；`assets/config/narrative.json`（门洞发现 toast「东侧墙体破开一道裂口，夜风灌入……」）；`scripts/tests/SmokeTest.gd`（新增断言：天空模式生效/围墙生成/门洞已开 `is_wall(167,32)==false`/庭院角墙存在/小地图跟随激活）。
  - **自动生效无需改动（已逐一代码核实）**：围墙碰撞体（`_build_collision` 遍历 buckets）、子弹被新墙阻挡、敌人 AI 视线与寻路（`wall_cells` 自动含新墙）、无限地面碰撞、小地图尺寸自适应（按最远墙格计算）、敌人红点在庭院内定位。全材质 UNSHADED 无光源依赖，切夜空不打乱现有渲染。
  - **已知风险（规划文档预判）**：现有距离雾参数（fog_light_color 0.08/0.09/0.12 + density 0.012）为封闭走廊设计，室外看夜空可能糊成灰蓝，需实机微调；墙顶面（BoxMesh 高 2.6m，unshaded）从室外仰视观感需实机确认。
  - **收尾项**：删除根目录临时勘察脚本 `tools/p2a_survey_tmp.py`（BFS 选址一次性工具，用后即删）。回归命令 `godot --path godot --headless res://scenes/tests/SmokeTest.tscn`（Godot 经 WinGet 安装，真实进程名 `Godot_v4.6.2-stable_win64`；若 PATH 无 godot 命令先探测，找不到再向用户要路径）。
- **2026-08-11** — **M3/P3 表现补齐：第三武器霰弹枪 + 12 号霰弹**（dev1 实机验收后合入 master）。新增泵动式弹匣霰弹枪：基础 8 发、每枪 6 弹丸、单丸伤害 20、散布随射程增大；通用组件逐阶提升为 12/16/24 发、7/8/10 弹丸、25/30/40 单丸伤害，二阶转半自动并取消泵动，三阶转 3.0 低速全自动。第一关拾取点位于北部仓库，双图配置生成独立 12 号霰弹补给。第一人称素材按现有手枪/冲锋枪硬边像素风对齐，枪口居中、右手握把、左手握泵，最终修复左臂裁切并使用底部对齐的专用 HUD 显示框；高清源与处理脚本已纳入版本库
- **2026-08-09** — **M3 敌人受击/死亡表现 + 双武器动作**（dev1，用户实机验收通过）。敌人增加受击白闪、上抬挤压回弹，以及倒地压扁→尸体停留→闪烁→淡出的死亡状态机；死亡后立即关闭碰撞与攻击。手枪/冲锋枪基于现有正式视图模型增加不同的射击后坐和换弹轨迹、换弹状态提示，并遵守暂停体系。死亡时序进入 `enemies.json` 配置；逐帧手部美术保留为后续可替换资源位；SmokeTest 扩至 53 断言，覆盖死亡碰撞/完整清理与武器动画启动
- **2026-08-09** — **结算时停修复 + 防护服 50% 减伤 + 道具平衡**（dev1，验收后合入 master）。根因修复：Main 为 PROCESS_MODE_ALWAYS 致整个 Viewport 子树（玩家/敌人/子弹）继承永不暂停——Viewport 显式改 PAUSABLE，一行覆盖结算/暂停菜单/简报三场景（暂停菜单期间敌人其实在动的隐患一并消除）；防护服能量改 50% 减伤（`armorAbsorbRate: 0.5` 配置化，1 能量抵消 1 点被吸收伤害）；道具平衡：w 死符号位改生电池 + 每第 4 个 H 改生电池（map0 电池 5→37/急救包 109→82，map1 3→17/45→34）；SmokeTest 扩至 49 断言（含电池/急救包精确计数与通关后注入射击弹药不变的暂停回归）
- **2026-08-09** — **P1 小地图 + P4 剧情改写**（dev1，验收后合入 master）。HUD 右上角数据驱动俯视小地图（墙体预渲染纹理+玩家箭头/敌人/信标，显式定位+resize 跟随；踩坑：CanvasLayer 直接子节点锚点不按视口解析、符号地图实际 69 行超 HEIGHT 常量）；剧情改诺亚生物科技生物兵器失控+特遣队特工（简报/结算/区域提示/README）
- **2026-08-09** — **三阶组件 + 道具体系重构**（dev1，验收后合入 master）。组件三阶：手枪 33 发+全自动 / 冲锋枪 60 发，伤害改 36/45/55，每阶射速 +10 封顶 40（合并神经加速器职能）；二阶紫/三阶金黄素材；"a 扩展弹匣""w 神经加速器"实机移除（生成层拦截，地图数据不动），"a"符号位改生**防护服能量**（Battery.png，+10，HUD 防护条，优先承伤）；急救包/电池满额拒收；组件分散放置（map0 西→东 / map1 同）；弹药箱换用 Upgrade Ammo 10.png；修复设置滑条拖动重置最大化窗口（Settings.apply 无条件 window_set_mode）；SmokeTest 45 断言
- **2026-08-09** — **通用武器升级组件（一/二级）+ 精英变异体改名**（dev1 开发，用户实机验收后合入 master）。weapons.json 新增 `upgradeComponents.levels/spawns` 配置驱动；顺序约束（越级拾取拦截+toast，拾取物保留）；`enemy_names` 变异体小BOSS→精英变异体；SmokeTest 24→33 断言并改物理帧驱动（headless 渲染/物理帧不同步踩坑）；README 武器/拾取物表同步
- **2026-08-08** `9b4acf4` — 新增 STATUS.md 进度追踪体系（本文件），CLAUDE.md / README.md 加入口指引；此前 `399b3ae` 新增 README（门面文档）、项目更名 Signal Lost（原 Retro FPS），仓库首次推送 GitHub
- **2026-08-08** `47ac39a` — **Phase 3：冲锋枪场景拾取 + Q/滚轮切换**。owned 持有模型（初始仅手枪）；`pickupSpawns` 配置生成 W 拾取物（map0 @ (20,35) / map1 @ (14,10)，不动 C++ 同源地图）；`grant_weapon` 首拾补满弹匣+自动切换，重复拾取转 30 备弹；移除 1/2 键；拾取精灵 48×41 金色光晕版；SmokeTest 扩至 24 断言
- **2026-08-08** `aa6ce5d` — 双击启动器 `启动Godot版.bat`（自动定位 WinGet 安装的 Godot 真实 exe）
- **2026-08** `5c3b3ce` — **Phase 2：双武器系统 + 数值重构 + 敌人攻击帧**。手枪半自动/冲锋枪全自动；血量 100→升级 120，敌血 80/100/300，伤害 32，敌伤×5；敌人开火切持枪攻击贴图
- **2026-08** `bfbc494` — AI 生成素材全套升级（菜单背景/墙地纹理/HUD/敌人拾取精灵）
- **2026-08** `ea39f5d` — 产品层补齐（菜单/简报/HUD/音效集成）
- **2026-08** `2aa1fb7` — 路线 B 原型（真 3D + 复古呈现层验证）
- **保护点** `d5a42a1` — C++ 版 v0.6.1 冻结归档，此后 C++ 侧不再演进

## 待办 / 下一步

1. **【进行中】P2a 实机验收收尾**（`dev1` @ `f6bbcc6`）：用户已试玩、评价室外风格可以，待正式确认；**明确批准后才合 master**
2. **【下一步动作】混合路线阶段一：外立面 + 庭院道具**（P2a 验收后动工，蓝图见 `docs/plans/2026-08-26-外景拓展与多层建筑混合路线规划.md`）：新增 2 张外立面贴图（AI 生成 + 用户确认）+ 首批道具精灵 → `level_ext.json` facade/props 段 → `LevelGenerator` 装饰生成（不进逻辑层）→ SmokeTest 新增断言 → 实机验收
3. P2b 斜坡/楼梯 + 敌人地面吸附（阶段二前置；见 `docs/plans/2026-08-09-godot-下一阶段规划.md`）；随后阶段二可玩二层夹层（层叠数据模型）
4. 武器拾取音效目前复用 `Energy Orb.ogg`，待用户提供专属音效偏好
5. 候选方向（待用户拍板）：更多武器/敌人种类、难度选择接入 Godot 版、存档系统、更多地图

## 交接须知（Agent 必读）

**关键路径与工具链**

- Godot 4.6.2 经 WinGet 安装，真实进程名 `Godot_v4.6.2-stable_win64`（`Get-Process godot` 会假阴性）
- 回归命令：`godot --path godot --headless res://scenes/tests/SmokeTest.tscn`（77 项全绿；历史上"手枪测试弹命中敌人"一项偶发时序不稳，近次回归已通过）
- C++ 版回归：`ctest --test-dir build -C Debug --output-on-failure`（127 测试）

**硬性约束**

- `godot/scripts/levels/LevelData.gd` 自动生成，**不可手改**；关卡数值改动走 `assets/config/*.json`
- 不修改 `prototypes/`、`src-legacy/`；C++ CMake target 名 `RetroFPS`（exe 名）属受保护构建产物，不动
- 提交消息以 `自检: ①②③④ 通过` 结尾（仓库既有风格）
- **用户驱动协作**：Write/Edit 前需用户确认，未经指示不提交
- **合并红线（用户明令）**：功能一律先在 `dev1` 开发并让用户实机体验，**未经用户明确批准严禁合入 master**

**已验证的技术方案（避免重复踩坑）**

- **暂停体系**：`Main`=PROCESS_MODE_ALWAYS（处理 UI/输入），`Viewport`=显式 PAUSABLE——游戏实体（玩家/敌人/子弹/拾取物）必须挂在 Viewport 子树内，挂到 Main 直下会继承 ALWAYS 导致 paused 时仍在跑（2026-08-09 结算时停 BUG 的根因）
- **MultiMesh 墙体包围盒剔除**：LevelGenerator `_build_walls` 用包围盒做剔除，向原地图矩形外追加的墙体若不同步扩大包围盒将**静默不显示**（P2a 已改为按实际墙格计算包围盒）
- **原边界墙格改造（开门洞）**：把原图中的墙格改造成通道，必须先从 `wall_cells` 与 buckets 中 erase 原墙（渲染/碰撞/小地图全从中派生，只登记新状态不移除旧状态 = 门三层都打不开）；围墙环与原图已有墙重叠的格应跳过追加，避免重复网格/碰撞体
- **多层建筑约束勘察（2026-08-26）**：敌人侦测/攻击距离为水平投影（忽略 y），玩家 CharacterBody3D 无跳跃键、≤45° 坡可行——可玩多层必须排在 P2b 斜坡/楼梯+地面吸附之后；三层**外观**可先用装饰外立面兑现（不进 `wall_cells`/buckets → 碰撞/AI/子弹/小地图零改动）；子弹为 3D 物理飞行，楼板有碰撞即天然遮挡
- **地图勘察方法**：直接解析 LevelData.gd 字符地图做 BFS 连通性/选址验证即可，不必启动游戏（脚本曾存根目录 `tools/p2a_survey_tmp.py`，属一次性工具，P2a 实施时删除）
- 脉冲输入（Q/滚轮）测试：`Input.parse_input_event` 注入 + `await get_tree().physics_frame` 等帧
- 窗口实证：SubViewport `get_texture().get_image().save_png()` 拿像素级渲染，比窗口截屏可靠
- 视线验证：截图/观察前先用 `PhysicsRayQueryParameters3D` 射线确认相机与目标间无墙体遮挡（排除玩家/目标自身 RID）

**文档索引**

| 文件 | 内容 |
| --- | --- |
| `STATUS.md`（本文件） | 进度快照与交接入口 |
| `PROJECT.md` | 项目全量上下文（架构/Agent 团队/规则/钩子，C++ 时代编写） |
| `CLAUDE.md` | Agent 配置索引 + 协作协议 |
| `README.md` | 对外门面 |
| `design/` · `docs/plans/` | 设计规格与开发计划（最新：`docs/plans/2026-08-26-外景拓展与多层建筑混合路线规划.md`） |
