# A批5 实施设计定稿（守门方 Kimi 亲自开发 · 2026-08-30）

> 依据：用户指令「塔楼放西北部大厅；二/三层做迷宫式多房间布局且 ≥ 一层 70%；一层南区旧坡道位急救包挪位」+ A批4r 验收反馈（楼梯卡顶/太窄/位置太靠后）。
> 本设计全部坐标经 LevelData 符号图逐格核验 + 连通性 BFS 实证（脚本实测，非行号换算）。

## 一、选址：西北部大厅

- 大厅范围 **cols 1-33 × rows 17-30**，边界：西 col0 地图边界墙 / 东 col34 墙 / 南 row31 墙 / 北 row16 墙（row16 有通往北区房间的门洞，塔墙立于 row17 内侧不影响一层通行）。
- 一层可行走 **428 格**（内部墙柱 34 格：含 (15-19,20)、(24,20-24) 等支撑柱——楼板压柱上方视觉自然）。
- 70% 目标 = 300 格。

## 二、塔楼结构

- 二层/三层 rect 均为 **x=1, y=17, w=33, h=14**（同尺寸无退台——用户"迷宫式+≥70%"最新口径覆盖旧退台拍板；三层东墙顶 7.8m 依旧对齐逻辑由顶板高度保证）。
- 二层可走 **313 格 = 73.1%** ✓；三层 **316 格 = 73.8%** ✓（BFS 自楼梯落点全连通实证）。
- 层高 2.6m 不变：二层板 2.6 / 三层板 5.2 / 顶板 7.8。
- 天花板：2.6 平面挖塔区（最外层 rect）；二层暴露区 = ∅（三层同 footprint 全覆盖）→ 无需 5.2 环带；三层顶板 7.8 整块（沿用 Ceiling 节点名）；楼梯井露天由板洞天然处理。既有逐层暴露区逻辑保持通用不删。

### 楼梯间（两层同井，cols 29-33 × rows 20-27）

- 井道内净区 cols 30-32 × rows 21-26；围合墙：
  - 西 col29 rows 20-27（门洞 (29,23)，三层同位对齐）——**一层为新增 runtime 隔墙**；
  - 北 row20 cols 29-32——**一层新增**；
  - 南 row27 cols 30-33 一层已有墙（col29,27 一层新增补齐）；东 col34 现成。
- 楼梯（沿用 4r 机制：可视台阶+斜坡碰撞，8 级/级高 0.325m/踏深 0.5m，宽 2 格）：
  - 楼梯1（一→二）：x=31, y=24, w=2, h=2, dir=N, base=0, height=2.6 → 占地 cols31-32×rows24-25，顶在 row24，二层落点 (31-32, 23)。二层板洞 = (31,24),(32,24),(31,25),(32,25)。一层路径：门 (29,23) → col30 行走带南行至 row26 → 东折上梯。
  - 楼梯2（二→三）：x=31, y=21, w=2, h=2, dir=S, base=2.6, height=2.6 → 占地 cols31-32×rows21-22，顶在 row22，三层落点 (31-32, 23)。三层板洞 = (31,21),(32,21),(31,22),(31? 否),(32,22)。二层路径：落点 (31-32,23) → col30 北行至 row21 → 东折上梯（双跑折返）。
- **卡顶修复**：斜坡碰撞顶端水平外延 0.15m 搭到楼板面（越过板洞边缘），消除顶沿卡壳；另查顶层台阶可视体勿与胶囊干涉。

### 一层新增隔墙（partitions，共 11 格，runtime 几何桶，LevelData 零触碰）

col29 × rows 20,21,22,24,25,26,27（门洞留 (29,23)）+ row20 × cols 29,30,31,32。
全部经核验为空地格、不压任何符号/道具；进 wall_cells + buckets + 碰撞 + 小地图。墙计数不变式 4197 → **4208**（4197+11，断言按 4197+partitions 配置数推导）。

## 三、层迷宫布局（grid 字符串，原点 (1,17)，33×14）

### 二层 grid（房间：A 西北上/B 西北下/C 中上/D 中下/E 东北上/F 东北下+井道；门：A↔C(10,21) B↔D(10,27) A↔B(6,24) C↔D(15,23) C↔E(20,19) D↔F(20,25) E↔F(25,21) F↔井道(29,23)）

```
XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX
X        X         X            X
X        X                      X
X        X         X        XXXXX
X                  XXXXX XXXX   X
X        X         X        X   X
X        XXXXX XXXXX            X
XXXXX XXXX         X        X   X
X        X                  X   X
X        X         X        X   X
X                  X        XXXXX
X        X         X            X
X        X         X            X
XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX
```

### 三层 grid（房间布局与二层不同；门：A↔B(8,22) A↔C(14,20) B↔D(14,26) C↔D(19,25) C↔E(24,22) D↔E(24,28) E↔井道(29,23)）

```
XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX
X            X         X        X
X            X         X        X
X                      X    XXXXX
X            X         X    X   X
XXXXXXX XXXXXX              X   X
X            X         X        X
X            X         X    X   X
X            XXXXX XXXXX    X   X
X                      X    X   X
X            X         X    XXXXX
X            X                  X
X            X         X        X
XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX
```

（注：井道板洞格在 grid 中留空，由 slabHoles 配置控制。）

## 四、层实体

- 二层：哨戒敌 1 个 id=1 @ (25,27)（F 房间）；拾取 h @ (4,19)、C @ (17,27)（沿用 h/C 避开一楼 H/e 计数断言口径）。
- 三层：空置（宝库物资归 A批4.5，待本批验收后由守门方更新 4.5 交底坐标再发令）。

## 五、急救包挪位（pickupOverrides 新机制）

- level_ext 新增 `pickupOverrides` 段：`[{map:0, suppress:{x:153,y:30}, place:{x:150,y:30,symbol:"H"}}]`。
- 生成器：符号图驱动的一层拾取生成时跳过 suppress 坐标；按 place 落配置拾取。(150,30) 已核验空地。
- 机制做通用（数组可多条），不只硬编码这一处。

## 六、旧东侧仓库多层拆除

- 删除 layers 两个旧条目（col154-166/col155-166）、stairs 两个旧条目；terrain 段不动（庭院土丘保留）。
- 东侧仓库恢复单层：2.6m 天花板自动恢复整片（层区挖洞逻辑只认 layers 配置）。
- 一层仓库区物品/敌人原样（LevelData 未动）。

## 七、测试要求（SmokeTest 改按新配置推导）

- 层几何：墙/板数量从 grid+slabHoles 推导；二层 313 / 三层 316 可走格（由配置算，不硬编码）；楼板顶面 2.60/5.20 射线；顶板 7.80。
- 楼梯：2 部、各 8 级、级高 0.325、贴图可加载、梯顶与目标层板等高衔接；**三角度实走上行断言**（正面/左偏/右偏，两部楼梯同测，到顶不滞留）。
- 隔墙：墙碰撞体数 == 4197+11；隔墙格在 wall_cells；(28,23)→BFS 一层大厅连通（扣除被围井道后的可达格数断言）。
- 天花板：塔区上方 2.6 平面无板（射线）、三层顶板 7.8、楼梯井头空达标。
- 拾取覆盖：(153,30) 无拾取、(150,30) 有 H。
- 旧仓库相关层断言全部移除；回归全绿（预期 153 ± 增减，交接报实际数与增减明细）。

## 八、纪律

- 分支 dev1；LevelData.gd 零触碰；改动限 level_ext.json + LevelGenerator.gd + SmokeTest.gd。
- 无新美术资产（楼梯贴图沿用 Stair Steps.bmp）→ 无 .import 风险。
- 提交消息规范：Write msgfile + `git commit -F`，消息以 `自检: ①②③④ 通过` 结尾，msgfile 用后删。
- 交接报告：实测数字（面积/覆盖率/回归数）、卡顶根因与修法、实机验收点。
