【A批5 · 多层塔楼迁址前中期 + 楼梯间化 + 楼梯卡顶修复】基于 dev1 @ a699522，A批4r 实机验收未过项修订，先于 A批4.5 执行。

用户实测反馈三条，逐条修复：

一、多层塔楼整体迁址（用户原话："楼梯间应该放在一楼地图前中期而不是后期的仓库出口附近"）
- 二/三层 + 楼梯从东侧仓库（col154-166×y25-34）迁往 map0 前中期主路径附近的既有房间；
  东侧仓库恢复单层（层配置/天花板洞/楼梯全部移除，2.6m 天花板恢复整片）。
- 候选选址（守门方看符号图初筛，最终以 wall_cells/地板格探针实证为准，禁止行号换算）：
  ① 中央大厅 col63-90×row17-26 一带（中期，两大实体块之间开阔厅，主路径上）——优先；
  ② 西南大厅 col3-28×row50-61（前期，出生点南侧）——备选。
- 选址要求：室内可用 ≥80 格（保二/三层内容量）；贴近主路径；不压现有 quest 关键物
  （选址内原有拾取/敌人可微调落位，交接逐项报备）；新址上方无冲突几何。
- 覆盖率口径沿用：二层 ≥70%、三层 ⊆ 二层子集且尽量 ≥70%（新房间若容不下，以"墙到墙铺满"
  为口径并在交接中给实测数字，由用户定夺）。
- 迁址后重报：新址一层实测面积、二/三层覆盖率、新址坐标。

二、楼梯间化（用户原话："应该是弄出一个楼梯间式的地方"）
- 楼梯改为独立围合的楼梯间：一层隔墙围合小空间 + 门洞进出；隔墙走 runtime 几何桶
  （庭院外扩已有先例，不触 LevelData 红线）；上层对应位置为楼梯井围合。
- 梯宽 ≥2 格、建议 3 格；可直跑可双跑（按选址房间形状定）；踏步 8 级/层、级高 0.325m 不变，
  碰撞仍走平滑斜坡（楼梯外观+坡道行走）；楼梯贴图沿用 Stair Steps.bmp。

三、楼梯卡顶修复（用户原话："上到最上边有时候会被卡住无法继续走"）
- 排查方向：①斜坡碰撞顶端与楼板洞边咬合不足——斜坡顶端应越过板洞边缘 0.1-0.2m 搭到楼板面；
  ②顶层台阶可视体与玩家胶囊干涉；③板洞边缘直角卡壳（可加小豁口/倒角）。
- 修复后新增断言：玩家从梯底以正面、左偏 30°、右偏 30° 三个角度实走上行，
  均能到达上层楼板面（y 增量达标且终点不在洞内滞留）。一→二、二→三两部楼梯同测。

四、测试与交接
- 层几何/天花板/楼梯断言全部改按新址配置推导；旧仓库层相关断言移除；回归全绿。
- 交接报告：新址选择与理由、卡顶根因与修法、实测面积口径与覆盖率、回归数、实机验收点
  （楼梯间进出顺畅、三角度上梯不卡顶、二楼抬头天花板完整仅露台见夜空）。
- 红线不变：LevelData 零触碰；改动限于 level_ext.json + LevelGenerator + SmokeTest（+如有需要的美术参数）。
- 注意：dev1 工作区若有守门方临时出生点补丁（注释"临时验收补丁·勿提交"）请勿带入提交。

五、后续联动（守门方负责，无需施工）
- A批4.5 设计文档（垂直动线与物资重排）的落位坐标将在新址确定后由守门方更新再发令。
