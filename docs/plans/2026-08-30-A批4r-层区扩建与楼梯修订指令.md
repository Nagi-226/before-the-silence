【A批4r · 层区扩建 + 天花板修补 + 正经楼梯】基于 dev1 @ 953e502，验收未过项修订，先于 A批4.5 执行。

用户实测反馈三条，逐条修复：

一、二/三层面积扩至 ≥ 一楼 70%
- 先实测一层仓库主房间（L形: 北区 col156-166×row24-28 / row29 连接段 / 南区 col153-166×row30-33，
  守门方粗测 ≈120 格，请以你的实测为准并在交接中给出数字与口径）。
- 二层扩至尽量贴一层主房间投影（允许拆多个 rect 拼 L 形），覆盖率 ≥70%、目标墙到墙；
  扣除楼梯井开口/必要挑空。层墙不得压一层门洞、不得压东侧裂口。
- 三层保持退台形态（仍略小于二层、露台可收窄为一条），但面积同样 ≥ 一楼 70%——
  用户最新口径："二楼和三楼至少有一楼面积的70%左右"（覆盖此前"三楼更小"的拍板，退台只作象征保留）。
- 层高 2.6m 不变；二层哨戒敌与层上拾取落位随新几何校验。

二、上下层改用正经楼梯（替代斜坡木板，用户原话"不要直接靠木板"）
- 实现口径（交底）：踏步用逐级可视体块（BoxMesh 台阶，踏面宽≥1格/每级升高≤0.325m 即 8 级/层），
  碰撞保留平滑斜坡（楼梯外观+坡道行走，业界常规做法，避免纯踏步碰撞卡脚/滑步）——
  现有"50 帧爬坡实走"类断言因此无需推翻，仅需按新位置更新。
- 楼梯贴图启用（原 A3 决策的批 4 美术项，落地到本批）：踏步侧面/踏面贴楼梯材质，
  新贴图入库必须 .import 随行（先 --headless --import 再 git add，B批2 已踩过坑）。
- 楼梯井：穿板洞尺寸随楼梯洞口同步；一→二、二→三各一部楼梯，位置结合新层布局选
  不压一层门洞/物品、不压裂口的墙边；楼梯底/顶落板等高衔接（沿用射线证明口径）。
- map0 庭院土丘、map2 信标土丘的室外土坡不在本批范围（那是地形坡，用户指的是楼层间上下）。

三、天花板按层修补（修 bug）
- 现状：2.6m 平面按最外层层区挖洞，但顶板仅生成最高层(7.8m)，导致二层未被三层覆盖区全露天。
- 改为逐层生成环形顶板：每层"本层 rect 减去上一层 rect"的区域在该层顶面高度生成顶板
  （二层暴露区→5.2m 顶板；三层→7.8m），仅设计内露台段保持露天。
- 沿用 Ceiling 节点名兼容 P2a 屋檐断言；outdoor 地图逻辑不受影响。

四、测试与交接
- SmokeTest：层几何数量断言改按新配置推导；新增"二层非露台暴露区上方 5.2m 顶板射线命中"断言、
  "二层西条不再露天"断言；保留露台露天断言；楼梯实走贯通断言（沿梯上行 y 增量达标）。
- 交接报告：一层实测面积与口径、二/三层覆盖率数字、楼梯级数/坡度参数、回归全绿、实机验收点
  （楼梯上下不卡脚、二楼抬头天花板完整仅露台见夜空、二楼环行不踩空）。
- 红线不变：LevelData 零触碰；改动限于 level_ext.json(layers/terrain) + LevelGenerator + SmokeTest + 楼梯贴图资产。
- 注意：dev1 工作区现有守门方临时出生点补丁（LevelGenerator.gd build() 末尾，注释"临时验收补丁·勿提交"），
  请勿把它带进提交；守门方会在下次提交前自行撤销。
